# Cinelog

REST API for a movie rating platform. Built with FastAPI, PostgreSQL, JWT authentication, Alembic migrations, and GitHub Actions CI.

## Stack
- FastAPI + Pydantic
- PostgreSQL + psycopg3 + connection pool
- JWT authentication
- Alembic migrations
- Docker + docker-compose
- Pytest + GitHub Actions CI